---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Socials from "@/components/Socials.astro";
import { SITE } from "@/config";
import { SOCIALS } from "@/constants";
---

<Layout>
  <Header />
  <main id="main-content" data-layout="index">
    <section id="hero" class="pt-8 pb-6">
      <h1 class="my-3 inline-block text-4xl font-bold sm:my-6 sm:text-5xl">
        Hello, I'm {SITE.author.split(" ")[1]}
      </h1>

      <p class="mt-2 text-2xl font-medium text-accent">Software Engineer</p>

      <p class="mt-4">
        I help companies build robust, cloud-native backend systems that are
        observable, performant, and scalable — enabling them to exceed their
        technical and business goals.
      </p>

      <p class="mt-3">
        I specialize in <strong>Golang</strong> development and have hands-on experience
        with <strong>Node.js</strong> and <strong>Python</strong>, building
        modular
        <strong>microservices</strong> with <strong>REST APIs</strong> that serve
        millions of users.
      </p>

      <p class="mt-3">
        I've contributed to critical public service platforms, powering digital
        services for over <strong>5 million users</strong> across <strong>
          in West Java, Indonesia</strong
        >.
      </p>

      {
        SOCIALS.length > 0 && (
          <div class="mt-4 flex flex-col sm:flex-row sm:items-center">
            <div class="mr-2 mb-1 whitespace-nowrap sm:mb-0">Social Links:</div>
            <Socials />
          </div>
        )
      }
    </section>
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>
